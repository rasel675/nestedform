<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Phone:</strong>
  <%= @user.phone %>
</p>

<h2>Addresses:</h2>
<ul>
  <% @user.addresses.each do |address| %>
    <li>
      <%= address.address %>, <%= address.district %>, <%= address.country %>
    </li>
  <% end %>
</ul>
